package Tile;

import java.awt.image.BufferedImage; // สำหรับจัดการรูปภาพ

public class Tile {
	
	public BufferedImage image; // ตัวแปรสำหรับเก็บรูปภาพของ Tile
	public boolean collision = false; // ตัวแปร boolean สำหรับบอกว่า Tile นี้สามารถชนได้หรือไม่ (ถ้า true คือชนได้)
}